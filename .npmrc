# This prevents npm from creating nested node_modules
hoist-pattern[]=*

# This prevents issues with peer dependencies
resolution-mode=highest
save-workspaces=true

# Prevent creation of package-lock.json files in subdirectories
package-lock=false
legacy-peer-deps=true
# This prevents npm from creating a package-lock.json file

